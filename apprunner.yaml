version: 0.1
os: linux
runtime: python
web:
  commands:
    - pip install --upgrade pip
    - pip install -r requirements.txt
    - gunicorn Hello_db:app --bind=0.0.0.0:$PORT --workers=4 --worker-class=gevent
